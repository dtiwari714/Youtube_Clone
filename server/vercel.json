{
  "version": 2,
  "builds": [{ "src": "./index.js", "use": "@vercel/static-build" }],
  "routes": [
    { "src": "/api/(.*)", "dest": "server.js" }
  ],
  "rewrites": [{ "source": "/(.*)", "destination": "/" }]
}
